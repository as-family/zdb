syntax = "proto3";

package zdb.proto;

enum ErrorCode {
    UNKNOWN = 0;
    NOT_FOUND = 1;
    INVALID_ARG = 2;
}

message ErrorDetails {
    ErrorCode code = 1;
    string what = 2;
    string key = 3;
    string value = 4;
    uint64 version = 5;
}
